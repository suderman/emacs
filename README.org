#+title: suderman/emacs 🐈
#+subtitle: My Emacs 31 configuration for NixOS and Android
#+author: Jon Suderman
#+options: toc:2 num:nil
#+startup: content

* About

This is the Emacs config I use every day. I tune it around how I edit, switch
projects, and manage windows. It is not a starter kit. It has my paths, my
keymap, and a few experiments that earned their place by solving something
annoying.

[[https://github.com/meow-edit/meow][Meow]] controls modal editing. Built-in =project.el= supplies project data.
IBuffer shows open buffers by project, Dirvish handles the file tree and file
management, and Magit handles Git. Vertico, Orderless, Consult, Marginalia, and
Embark handle minibuffer completion.

Some choices are a little odd on purpose. Comma opens IBuffer, period opens
Dirvish, and apostrophe repeats edits. =SPC= is both Meow's keypad and my leader
key. IBuffer and Dirvish pass file and project context back and forth instead of
acting like separate tools.

The editing map starts with selections, then applies changes to them. Dirvish
cleans up Dired. Markdown preview runs Pandoc and refreshes after each save.
Stylix supplies the colors, while Doom Modeline and Nerd Icons handle the lower
status bar and file symbols. Image mode cycles through a directory, transforms
images in place, and starts animated images automatically.

* Run it

The flake packages Emacs 31 PGTK with this configuration, its Elisp packages,
native modules, tree-sitter grammars, language servers, formatters, and
command-line tools.

#+begin_src sh
nix run github:suderman/emacs
#+end_src

That runs the immutable configuration bundled in the Nix store. From a local
checkout, I can build and run the same package with:

#+begin_src sh
nix run .
#+end_src

To use the flake's dependencies while loading the writable checkout instead:

#+begin_src sh
nix run . -- --init-directory "$PWD"
#+end_src

After that, =F5= reloads the modular configuration without rebuilding the
package.

The flake exposes =x86_64-linux= and =aarch64-linux= packages. I use and test
the x86_64 build.

* Android

The same checkout also runs in native graphical GNU Emacs on Android. The APK
is signed with the public certificate used by official Termux GitHub releases,
so Android grants Emacs the APK's =com.termux= shared UID and Emacs can execute
programs under =/data/data/com.termux/files/usr/bin=.

The Android build currently targets Emacs 31.0.50, Android API 35 or newer, and
=arm64-v8a=. Build it on x86_64 Linux with:

#+begin_src sh
nix build .#emacs-android --out-link result-android
#+end_src

The result is
=result-android/emacs-31.0.50-35-arm64-v8a-termux.apk=. The flake checks the
package, version, minimum and target APIs, shared UID, debuggable manifest flag,
ABI, certificate, unchanged manifest, and 16 KiB ZIP alignment. The Android
tools are also available in:

#+begin_src sh
nix develop .#android
#+end_src

** Guided installation

The installer builds and verifies the APK, waits for exactly one authorized
phone, checks its API, ABI, active profile, installed Termux certificate, and
any installed Emacs certificate, then asks before installing or updating:

#+begin_src sh
./android/install.sh
#+end_src

After Android verifies the installation, the installer discovers the app data
directory with =run-as org.gnu.emacs pwd=. It installs any missing pinned Nerd
Font files under =$HOME/fonts=, then checks that Termux Git runs from the Emacs
app context before offering to clone or update this repository at
=$HOME/.config/emacs=. A managed checkout must have =init.el=, a clean worktree,
the expected GitHub origin, a resolvable HEAD, and no local divergence. Updates
use =git pull --ff-only= and report the resulting commit.

Every configuration change needs explicit confirmation. The launch prompt
appears afterward, whether setup succeeds, is declined, or needs manual repair.
The installer never installs, removes, re-signs, or changes Termux or its
plugins. It also refuses to replace an Emacs app signed with another certificate
and refuses a downgrade.

Official Termux GitHub releases use a public test key. This APK must use that
same key to join Termux's shared UID. The expected certificate SHA-256 is:

#+begin_example
B6:DA:01:48:0E:EF:D5:FB:F2:CD:37:71:B8:D1:02:1E:
C7:91:30:4B:DD:6C:4B:F4:1D:3F:AA:BA:D4:8E:E5:E1
#+end_example

The key is not secret. Anyone can forge updates signed with it, so only install
an APK built from sources you trust. GitHub and F-Droid Termux builds use
different signing identities. Stop if the installed certificate does not match;
do not replace or uninstall Termux to work around it.

** Install the configuration

For a fresh install, the installer clones into an installer-owned temporary
directory and verifies it before moving it to =$HOME/.config/emacs=. It refuses
non-Git, nonempty, dirty, divergent, or wrong-origin targets without changing
them.

Emacs checks =$HOME/.emacs.d= before the XDG configuration. If that legacy path
remains, =$HOME/.config/emacs= will not load. The installer explains this and
asks before moving the directory to:

#+begin_example
$HOME/.local/state/emacs/legacy-emacs.d-YYYYMMDD-HHMMSS
#+end_example

It never deletes the legacy directory. During a fresh migration, cloning and
initial verification finish before the legacy directory moves. If a later move
or verification fails, the installer removes only its temporary or newly
installed checkout and restores =$HOME/.emacs.d=. With an existing managed
checkout, it updates first and asks separately before moving the legacy path.

If =run-as= or Termux Git is unavailable, setup is declined, or the target is
unsafe, the successful APK installation stands and the installer prints manual
recovery commands. Termux and Emacs share a UID, but they have different
=$HOME= values. Use the absolute Emacs home printed by the installer when
running the fallback from Termux. If =run-as= cannot discover it, open =M-x
shell= in Emacs and use =$HOME= there.

The complete fallback is:

#+begin_src sh
set -eu
EMACS_HOME='/path/printed/by/the/installer'
GIT=/data/data/com.termux/files/usr/bin/git
CONFIG="$EMACS_HOME/.config/emacs"
ORIGIN=https://github.com/suderman/emacs.git

mkdir -p "$EMACS_HOME/.config"
if [ -d "$CONFIG/.git" ]; then
  test -z "$("$GIT" -C "$CONFIG" status --porcelain)"
  case "$("$GIT" -C "$CONFIG" remote get-url origin)" in
    https://github.com/suderman/emacs|https://github.com/suderman/emacs.git) ;;
    *) printf '%s\n' "Refusing unexpected origin" >&2; exit 1 ;;
  esac
  "$GIT" -C "$CONFIG" pull --ff-only
elif [ -e "$CONFIG" ]; then
  printf '%s\n' "Refusing existing non-Git target: $CONFIG" >&2
  exit 1
else
  "$GIT" clone "$ORIGIN" "$CONFIG"
fi
test -f "$CONFIG/init.el"
test -z "$("$GIT" -C "$CONFIG" status --porcelain)"
"$GIT" -C "$CONFIG" rev-parse --verify HEAD

if [ -e "$EMACS_HOME/.emacs.d" ]; then
  BACKUP="$EMACS_HOME/.local/state/emacs/legacy-emacs.d-$(date +%Y%m%d-%H%M%S)"
  mkdir -p "$EMACS_HOME/.local/state/emacs"
  mv "$EMACS_HOME/.emacs.d" "$BACKUP"
  printf 'Legacy configuration moved to %s\n' "$BACKUP"
fi
#+end_src

The installer and these commands never modify Termux. Restart Emacs after setup.
The Android module adds Termux's binary directory to =PATH= and =exec-path=.
Keep =LD_LIBRARY_PATH= unset. Check the connection with =(executable-find
"git")=, =(executable-find "rg")=, and =(executable-find "fd")=.

Android stores Emacs's bundled data under the virtual =/assets= tree, which an
external GPG process cannot read. The package bootstrap copies the bundled GNU
ELPA keyring to a temporary physical file for import and deletes it afterward.
Package signature verification stays enabled.

Android keeps the portable editing core: Meow, completion, Org, Magit,
Dashboard, Dired and Dirvish, Markdown, and Image mode. Ghostel, Nix
tree-sitter integration, automatic formatting, Pi, PDF Tools, and Kitty graphics
remain desktop-only. Dashboard and Dirvish only show destinations present on
the current device. EPUB, audio, and video files open through Android's native
app chooser.

** Fonts and input

Before the launch prompt, the installer automatically downloads any missing
Nerd Fonts 3.4.0 files used by the desktop configuration. It checks their
SHA-256 hashes before and after transfer, stages the complete set, and only then
moves them under =$HOME/fonts=. Existing font files are never overwritten. A
failure leaves no partially installed set and does not invalidate the APK or
configuration setup. Fix the reported network or host-tool problem and rerun
=./android/install.sh=. Until then, the config uses the Android system font and
text labels instead of icon glyphs. Android scans =$HOME/fonts= at process
startup, so restart an already-running Emacs after a standalone setup retry.

Android uses an 18 point default font while desktop frames remain at 11 points.
The default face carries that size into ordinary buffers, modelines, and
minibuffers.

The bottom toolbar supplies Escape, Tab, Ctrl, and Meta. Volume Down applies
Ctrl to the next key, and Volume Up applies Meta. Press Volume Down then Volume
Up for Escape, or Volume Up then Volume Down for Tab. The toolbar Escape button
calls Meow's insert-exit command directly.

* Nix and mutability

I want deterministic bootstrapping without turning every Lisp edit into a Nix
rebuild. The split is fairly small.

- =use-package= forms remain the source of truth for pure Elisp dependencies.
  The flake reads those declarations and bundles the matching packages.
- Native Emacs modules, tree-sitter grammars, and external programs live in
  [[file:nix/packages/emacs.nix][nix/packages/emacs.nix]] because they need compiled or system-level
  dependencies.
- =early-init.el=, =init.el=, =lisp/=, and =assets/= become an immutable config
  source in the Nix store.
- =package.el= still works for quick experiments. Its packages live under
  =~/.local/share/emacs/elpa= instead of this repository.

My matching [[https://github.com/suderman/nixos][suderman/nixos]] flake owns the host-specific part. That includes the daemon
and client, Stylix theme and typography, icon fonts, system shortcuts, and
persistent state. Its Emacs wrapper prefers a writable
=~/.config/emacs/init.el= when one exists. Without that checkout, it falls back
to the configuration bundled by this flake. A direct =nix run= uses the bundled
copy by default.

Package data and runtime state follow XDG paths, so they do not end up in Git.

Read [[file:AGENTS.md][AGENTS.md]] before changing the structure. It explains which repository owns
each package, what each module does, where hot reload has failed before, and how
I test changes.

* Keymap reference

These are the keys I want close at hand. They cover the daily map without
copying every binding from the source. =SPC ?= opens Meow's full cheatsheet.
Which-Key shows each leader prefix while I type it.

** Finding a command

| Key       | Action                                  |
|-----------+-----------------------------------------|
| =SPC ?=   | Open the Meow cheatsheet                |
| =SPC ...= | Show the current leader choices         |
| =C-h k=   | Describe a key                          |
| =C-h b=   | Show effective bindings in this buffer  |
| =M-x=     | Run a command by name                   |
| =F5=      | Reload the modular configuration        |

** Meow normal state

*** Movement and navigation

| Keys        | Action                                         |
|-------------+------------------------------------------------|
| =h j k l=   | Move left, down, up, and right                  |
| =a / e=     | Move to smart beginning / end of line           |
| =A / Z=     | Move to beginning / end of buffer               |
| =b / f=     | Move backward / forward by word                 |
| =B / F=     | Move backward / forward by symbol               |
| =#=         | Go to line                                      |
| =%=         | Jump to matching delimiter or item              |
| =C / V=     | Page up / down                                  |
| =, / .=     | Open project-grouped IBuffer / Dirvish          |
| =`=         | Open the Dashboard                               |
| =S=         | Toggle the Dirvish sidebar without moving focus  |
| =q / Q=     | Quit the view / kill the current buffer         |

Movement expands an active Meow selection.

=S= has the same visibility-only behavior in Meow Motion. Inside the sidebar it
closes the sidebar; in IBuffer it uses the selected buffer or project.

*** Selection and editing

| Keys           | Action                                             |
|----------------+----------------------------------------------------|
| =m / mm / mmm= | Select word / symbol / enclosing block              |
| =M / MM / MMM= | Select rectangle / line / whole buffer               |
| =g / G=        | Cancel selection / grab another selection           |
| =i / I / E=    | Insert here / at indentation / at end of line        |
| =o / O=        | Open a line below / above                            |
| =c / v=        | Copy / paste                                         |
| =d / D=        | Delete selection or character / delete line          |
| =x / X=        | Cut and insert / cut line and insert                  |
| =r=            | Replace one character                                |
| =s=            | Surround command prefix                              |
| =s s=          | Add a surround                                       |
| =H / L=        | Outdent / indent                                     |
| =J / K=        | Move selected lines down / up                         |
| =u / U=        | One-way undo / one-way undo inside selection         |
| =y=            | Redo                                                 |
| ='=            | Repeat the last edit                                 |

=d= does not fill the kill ring. =x= cuts a real multi-character selection,
then enters Insert state.

*** Search and repeat

| Keys    | Action                                                   |
|---------+----------------------------------------------------------|
| =n / p= | Search forward / backward                               |
| =t / T= | Move till a character forward / backward                |
| ='=     | Repeat the last till immediately after =t= or =T=       |
| =/=     | Visit a selected thing                                  |

Repeat-FU records edits across Insert-state transitions and keeps repeat history
across buffers.

** SPC leader

=SPC= does two jobs. It opens Meow's keypad and my leader map. Numeric arguments
fit the same grammar. For example, =SPC 3 f= moves forward three words.

| Key       | Action                              |
|-----------+-------------------------------------|
| =SPC ?=   | Meow cheatsheet                     |
| =SPC SPC= | =M-x=                               |
| =SPC .=   | Git commands                        |
| =SPC b=   | Buffer commands                     |
| =SPC f=   | File commands                       |
| =SPC o=   | Org commands                        |
| =SPC s=   | Search commands                     |
| =SPC t=   | Toggles                             |
| =SPC w=   | Window commands                     |
| =SPC q=   | Quit and reload commands            |

I use these subcommands most often:

| Prefix  | Useful keys                                                   |
|---------+---------------------------------------------------------------|
| =SPC b= | =b= switch, =k= kill, =l= alternate, =r= revert, =s= save     |
| =SPC f= | =.= Dirvish, =f= find with consult-fd, =g= ripgrep, =r= recent, =s= save, =S= save as |
| =SPC .= | =.= status, =d= file diff, =l= file log, =h= hunks, =c= conflicts |
| =SPC o= | =a= agenda, =c= capture, =d/s= deadline/schedule, =g= headings, =i/l= insert/store link, =r= refile, =t/T= TODO item/list, =x= checkbox, =e= export |
| =SPC s= | =c= clear, =i= Imenu, =l= line                                |
| =SPC t= | =c= column indicator, =d= Dirvish sidebar, =h= current line, =l= line numbers, =o= Org indentation, =r= read only, =s= spelling, =v= visual lines, =w= whitespace |
| =SPC w= | =hjkl= focus, =HJKL= resize, =s/v= split, =o= keep one        |
| =SPC q= | =r= reload                                                     |

** Git

Magit is the repository dashboard. It stages files, hunks, and selected lines;
drives commits, branches, remotes, logs, diffs, stashes, rebases, and other Git
operations through its native Transient menus. Meow stays off in Magit buffers
so those native single-letter commands win.

| Key         | Action                                  |
|-------------+-----------------------------------------|
| =SPC . .=   | Open Magit status                       |
| =SPC . m=   | Open Magit dispatch                     |
| =SPC . f=   | Open current-file dispatch              |
| =SPC . d=   | Diff the current file                   |
| =SPC . l=   | Show current-file history               |
| =SPC . b=   | Show current-line blame                 |
| =SPC . B=   | Show full-buffer blame                  |

diff-hl updates change indicators while editing. Its hunk commands are grouped
under =SPC . h=.

| Key           | Action                         |
|---------------+--------------------------------|
| =SPC . h n/p= | Next / previous hunk           |
| =SPC . h v=   | Preview hunk                   |
| =SPC . h d=   | Open hunk in a diff            |
| =SPC . h s=   | Stage hunk                     |
| =SPC . h r=   | Revert hunk                    |
| =SPC . h u=   | Unstage the current file       |

Built-in smerge-mode and Ediff handle conflict markers. Upper and lower are
used instead of ours and theirs because those meanings reverse during rebase.

| Key           | Action                                  |
|---------------+-----------------------------------------|
| =SPC . c n/p= | Next / previous conflict                |
| =SPC . c u/l= | Keep upper / lower side                 |
| =SPC . c a=   | Keep all sides                          |
| =SPC . c b=   | Keep the base revision                  |
| =SPC . c 0=   | Keep neither side                       |
| =SPC . c r=   | Refine conflict highlighting            |
| =SPC . c e=   | Resolve the current conflict with Ediff |

Inside Magit, =j/k= and =n/p= move by section, =^= moves to the parent section,
=TAB= expands or collapses, =RET= visits, =h= opens the dispatch menu, and =q=
or =.= restores the previous buffer. The original =j= jump and =k= discard
commands remain available as =h j= and =h k=. In Transient menus, =ESC= returns
to the parent menu or closes the top-level menu; =C-q= exits the entire stack.

** Dashboard

New Emacs sessions and fileless =emacsclient= frames start on a small dashboard.
Its top row opens my common source and configuration directories through
Dirvish, followed by recent projects and recent files. Backtick returns to it
from Meow Normal and Motion states, IBuffer, or Dirvish. =M-x
suderman/dashboard= works everywhere without using the leader map.

| Key       | Action                                      |
|-----------+---------------------------------------------|
| =h / l=   | Move to the previous / next button in a row |
| =j / k=   | Move to the nearest button below / above    |
| =1 ... 8= | Open the matching pinned destination         |
| =TAB=     | Move to the next item                        |
| =RET=     | Open the item at point                       |
| =p / r=   | Jump to projects / recent files             |
| =s=       | Open =*scratch*=                             |
| =SPC=     | Open the Meow keypad                         |
| =, / .=   | Open IBuffer / Dirvish                       |
| =g=       | Refresh the dashboard                       |
| =q=       | Close the dashboard                         |

** IBuffer and the file sidebar

I use IBuffer to answer "what is open?" and the Dirvish sidebar to answer
"where is this file?" The sidebar follows the selected file and project while
leaving editor focus alone. Meow stays off in both views so their own maps win.

*** IBuffer

| Key       | Action                                                       |
|-----------+--------------------------------------------------------------|
| =j / k=   | Move by row                                                  |
| =l=       | Visit the selected buffer, or toggle a group heading         |
| =h / ,=   | Close IBuffer and restore the previous window               |
| =S=       | Toggle a contextual file sidebar without leaving IBuffer     |
| =m=       | Toggle the current mark without moving                       |
| =M=       | Mark every visible buffer                                    |
| =t=       | Invert marks; =M t= clears all marks                         |
| =u / U=   | Unmark without moving / unmark all                           |
| =.=       | Open Dirvish for the selected buffer or project              |
| =,=       | Close IBuffer and restore the previous window                |

Left click visits a buffer. Middle click toggles its mark. Non-project images
have their own group; other non-project buffers remain in the default group.

*** Dirvish sidebar

| Key        | Action                                         |
|------------+------------------------------------------------|
| =j / k=    | Move to the next / previous entry              |
| =h / l=    | Move to the parent / open the selected entry   |
| =TAB=      | Expand or collapse a directory subtree         |
| =S / q=    | Close the sidebar                              |
| =.=        | Close the sidebar                              |
| =,=        | Open IBuffer without closing the sidebar       |
| =i=        | Toggle dotfiles                                |
| =g=        | Refresh                                        |
| =?=        | Show the effective bindings                    |

The sidebar follows the active file and project and expands its parent
directories. =S= from an editor toggles visibility without stealing focus. =S=
from IBuffer uses the selected buffer or project heading as context.

** Dirvish and Image mode

| Context | Keys                  | Action                                      |
|---------+-----------------------+---------------------------------------------|
| Dirvish | =h / j / k / l=       | Parent / next / previous / open             |
| Dirvish | =f=                   | Toggle full-frame layout                    |
| Dirvish | =TAB=                 | Toggle directory subtree                   |
| Dirvish | =S=                   | Toggle file sidebar                        |
| Dirvish | =H / L=               | Directory history backward / forward        |
| Dirvish | =N / E / R=           | Narrow / file groups / rsync               |
| Dirvish | =m / M / t=           | Toggle mark / mark all / invert marks       |
| Dirvish | =u / U=               | Unmark without moving / unmark all          |
| Dirvish | =c / x / v=           | Stage copy / stage cut / paste              |
| Dirvish | =d / X=               | Flag for deletion / delete flagged files    |
| Dirvish | =a / r=               | Create / rename                             |
| Dirvish | =. / ,=               | Close Dirvish / replace it with IBuffer     |
| Dirvish | =g / I / i=           | Refresh / file information / toggle dotfiles |
| Dirvish | =s / z=               | Sort / quick access                         |
| Dirvish | =/ / ; / ?=           | Search here / Dirvish menu / current keys   |
| Dirvish | =SPC=                 | Meow keypad                                 |
| Dirvish | =q=                   | Close Dirvish                               |
| Image   | =n / p=               | Browse images in cyclic alphabetical order |
| Image   | =h=                   | Move left                                   |
| Image   | =, / .=               | Open IBuffer / Dirvish                      |
| Image   | =SPC=                 | Keep Meow's keypad                         |
| Image   | =+= / === / =-=       | Zoom in / zoom in / zoom out               |
| Image   | =r / R=               | Rotate clockwise / counterclockwise        |
| Image   | =0=                   | Reset image transformations                |

Dirvish =c= and =x= stage the marked files, or the current file when nothing is
marked. Copies remain staged after =v=; cuts clear once the move starts.
Period opens the full-frame layout when no other window is visible; with splits
or the sidebar it uses only the selected editor window. The =f= key toggles
between those layouts. Comma closes a full-frame layout before opening IBuffer.
From the sidebar it opens IBuffer in an editor window and leaves the sidebar
visible. Closing IBuffer restores the prior editor window.
Dotfiles start hidden in the parent and current panes, and =i= toggles them
together without status messages. The preview remains unfiltered. Deleting a
file kills its unmodified visiting buffer, while modified buffers still require
confirmation. Use =M-x revert-buffer= when another program changes the
directory. The =?= popup reads the current bindings instead of Dirvish's default
command menu. Semicolon opens the original Dirvish menu. Directory arrows toggle
subtrees on click, matching =TAB=.

Single left click selects a row, double left click opens it, and right click
opens Dired's file or directory context menu. PDF files open in PDF Tools; EPUB
files use the desktop reader, and audio and video use the desktop player. NixOS
currently maps those to Zathura and mpv. A single click in the center keeps focus
there instead of activating the preview. The left parent column is also
clickable: directories change the center column, while files change directory
and select that file. In that column, =j/k= move between directories and update
the center, =h= goes up another level, and =l= returns to the center. Breadcrumb
clicks run in the center, and =M-hjkl= skips breadcrumb and footer windows.

Dirvish previews video, images, looping GIFs, audio metadata, EPUB covers,
archives, fonts, and PDFs. Minibuffer file selection uses the same preview
dispatchers after a short delay. Animated images opened in Image mode also
autoplay and loop.

** Windows and frames

These bindings work in Meow Normal and Motion states, Magit, IBuffer, and
Dirvish.

| Key           | Action                              |
|---------------+-------------------------------------|
| =M-h/j/k/l=   | Focus the adjacent window           |
| =M-H/J/K/L=   | Move divider left/down/up/right       |
| =M-u / M-i=   | Split below / split right           |
| =M-w=         | Delete the window or current tab    |
| =M-z=         | Zoom / restore the selected window  |
| =s-+ / s--=   | Increase / decrease frame text size |
| =s-t=         | New tab                             |
| =s-[ / s-]=   | Previous / next tab                 |
| =s-w=         | Close tab                           |

* Repository map

| Path                            | Role                                             |
|---------------------------------+--------------------------------------------------|
| [[file:flake.nix][flake.nix]]                       | Flake inputs and Blueprint outputs               |
| =nix/=                          | Desktop and Android packages, shells, and checks |
| =android/install.sh=            | Guided Android APK installer                     |
| =assets/=                       | Dashboard and Markdown preview assets            |
| [[file:early-init.el][early-init.el]]                 | Process-level startup and frame defaults         |
| [[file:init.el][init.el]]                       | Boring dependency-ordered module loader          |
| =lisp/suderman-paths.el=        | XDG state paths                                  |
| =lisp/suderman-android.el=      | Android paths and input toolbar                  |
| =lisp/suderman-packages.el=     | =package.el= and =use-package= bootstrap          |
| =lisp/suderman-defaults.el=     | Small built-in behavior changes                  |
| =lisp/suderman-appearance.el=   | Fonts, faces, modeline, and visual guides        |
| =lisp/suderman-windows.el=      | Window helpers and smooth jump animation         |
| =lisp/suderman-meow.el=         | Modal editing and the Normal/Motion maps         |
| =lisp/suderman-images.el=       | Image navigation, transforms, and clipboard      |
| =lisp/suderman-buffers.el=      | Project-grouped IBuffer                          |
| =lisp/suderman-completion.el=   | Vertico, Orderless, Consult, Marginalia, Embark  |
| =lisp/suderman-files.el=        | Dired, Dirvish, sidebar, previews, and transfers |
| =lisp/suderman-dashboard.el=    | Startup dashboard and common destinations       |
| =lisp/suderman-markdown.el=     | Markdown editing and Pandoc preview              |
| =lisp/suderman-languages.el=    | Broad language associations and tree-sitter use  |
| =lisp/suderman-nix.el=          | Embedded-language highlighting in Nix strings    |
| =lisp/suderman-git.el=          | Magit, live hunks, and conflict handling          |
| =lisp/suderman-org.el=          | Org agenda, capture, and refile workflow           |
| =lisp/suderman-keys.el=         | Global, Meta, and =SPC= leader bindings          |
| =lisp/suderman-formatting.el=   | Project and buffer formatting commands           |
| =lisp/suderman-reload.el=       | Safe modular hot reload                          |
| =test/=                         | Focused ERT regression tests                     |
| [[file:AGENTS.md][AGENTS.md]]                   | Maintenance decisions and verification rules     |

The source wins when this file falls behind. This reference only covers the
parts I use often enough to want on one page.
